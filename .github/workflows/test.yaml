name: Tests
on:
  push:
    branches:
      - master
      - 'release-*'
  pull_request:
    branches:
      - master
      - 'release-*'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run tests against'
        type: environment
        required: true

env:
  TEST_USERNAME: admin
  TEST_PASSWORD: password
  CATTLE_BOOTSTRAP_PASSWORD: password
  # TEST_BASE_URL set dynamically in jobs
  # API set dynamically in jobs
  RANCHER_URL: "not set"
  RANCHER_IP: "not set"
  # Build the dashboard to use in tests. When set to false it will grab `latest` from CDN (useful for running e2e tests quickly)
  BUILD_DASHBOARD: true

  E2E_RANCHER_LOG: rancher.log
  E2E_BUILD_DIST_NAME: dist
  E2E_BUILD_DIST_DIR: dist
  E2E_BUILD_DIST_EMBER_NAME: dist_ember
  E2E_BUILD_DIST_EMBER_DIR: dist_ember

jobs:
  e2e-ui-build:
    if: "!contains( github.event.pull_request.labels.*.name, 'ci/skip-e2e')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - name: Setup env
        uses: ./.github/actions/setup
      - name: Build e2e
        run: yarn e2e:build
      - name: Upload e2e build
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ env.E2E_BUILD_DIST_NAME }}
          path: ${{ env.E2E_BUILD_DIST_DIR }}/
          if-no-files-found: error
          retention-days: 10
          compression-level: 9
      - name: Upload e2e build ember
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ env.E2E_BUILD_DIST_EMBER_NAME }}
          path: ${{ env.E2E_BUILD_DIST_EMBER_DIR }}/
          if-no-files-found: error
          retention-days: 10
          compression-level: 9

  e2e-run-rancher:
    if: "!contains( github.event.pull_request.labels.*.name, 'ci/skip-e2e')"
    runs-on: ubuntu-latest
    needs: e2e-ui-build
    strategy:
      fail-fast: false
      matrix:
        role: [
          { username: 'admin', tag: '@adminUser' },
          { username: 'standard_user', tag: '@standardUser' }
        ]
        features:  [
          ['@navigation', '@extensions'],
          ['@charts'],
          ['@explorer'],
          ['@explorer2'],
          ['@virtualizationMgmt', '@fleet'],
          ['@generic', '@globalSettings'],
          ['@manager'],
          ['@userMenu', '@usersAndAuths'],
          ['@components'],
          ['@noVai']
        ]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - name: Setup env
        uses: ./.github/actions/setup
      - name: Download e2e build
        uses:  actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
         name: ${{ env.E2E_BUILD_DIST_NAME }}
         path: ${{ env.E2E_BUILD_DIST_DIR }}
      - name: Download e2e build ember
        uses:  actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
         name: ${{ env.E2E_BUILD_DIST_EMBER_NAME }}
         path: ${{ env.E2E_BUILD_DIST_EMBER_DIR }}
      - name: Get Public IP and Start Rancher
        id: get_ip
        run: |
          IP=$(curl -s ifconfig.me)
          echo "RANCHER_IP=$IP" >> $GITHUB_ENV
          echo $IP > rancher_ip.txt
          TEST_BASE_URL=https://$IP.sslip.io nohup ./scripts/e2e-k3s-start.sh > ${{ env.E2E_RANCHER_LOG }} 2>&1 &
      - name: Upload IP
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: rancher-ip-${{ github.run_id }}-${{ matrix.role.username }}-${{ matrix.features[0] }}
          path: rancher_ip.txt
          retention-days: 1
      - name: Wait for Rancher to be ready
        run: |
          echo "Waiting for Rancher at https://${{ env.RANCHER_IP }}.sslip.io"
          timeout 5m bash -c 'until curl -k --silent --output /dev/null --head --fail "https://${{ env.RANCHER_IP }}.sslip.io/dashboard/"; do echo "waiting..."; sleep 5; done'
      - name: Keep alive and capture logs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Rancher is up. Waiting for the corresponding test job to finish."

          # Timeout after 1 hour
          end_time=$(( $(date +%s) + 3600 ))

          while [[ $(date +%s) -lt $end_time ]]; do
            # Check if the completion artifact exists
            ARTIFACT_ID=$(gh api "/repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts" --jq ".artifacts[] | select(.name == \"test-done-${{ github.run_id }}-${{ matrix.role.username }}-${{ matrix.features[0] }}\") | .id")
            if [[ -n "$ARTIFACT_ID" ]]; then
              echo "Test job has finished. Shutting down Rancher."
              exit 0
            fi

            echo "Test job not finished yet. Waiting..."

            # Capture Rancher logs and wait before next check
            yarn k3s:local:logs
            sleep 30
          done

          echo "Timed out waiting for test job to finish."
          exit 1
      - name: Fetch Rancher Logs
        run: |
          yarn k3s:local:logs
          mkdir -p artifacts
          cp ${{ env.E2E_RANCHER_LOG }} artifacts/rancher.logs 2>/dev/null || :
      - name: Upload Rancher Logs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{github.run_number}}-${{github.run_attempt}}-rancher-logs
          path: artifacts/
          retention-days: 5

  e2e-test:
    if: "!contains( github.event.pull_request.labels.*.name, 'ci/skip-e2e')"
    needs: e2e-ui-build
    strategy:
      fail-fast: false
      matrix:
        role: [
          { username: 'admin', tag: '@adminUser' },
          { username: 'standard_user', tag: '@standardUser' }
        ]
        features:  [
          ['@navigation', '@extensions'],
          ['@charts'],
          ['@explorer'],
          ['@explorer2'],
          ['@virtualizationMgmt', '@fleet'],
          ['@generic', '@globalSettings'],
          ['@manager'],
          ['@userMenu', '@usersAndAuths'],
          ['@components'],
          ['@noVai']
        ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - name: Setup env
        uses: ./.github/actions/setup
      - name: Wait for Rancher and get IP
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_ID: ${{ github.run_id }}
        run: |
          ARTIFACT_NAME="rancher-ip-${RUN_ID}-${{ matrix.role.username }}-${{ matrix.features[0] }}"
          echo "Looking for artifact: $ARTIFACT_NAME"
          for i in {1..60}; do
            ARTIFACT_ID=$(gh api "/repos/${{ github.repository }}/actions/runs/${RUN_ID}/artifacts" --jq ".artifacts[] | select(.name == \"$ARTIFACT_NAME\") | .id")
            if [[ -n "$ARTIFACT_ID" ]]; then
              echo "Artifact found with ID: $ARTIFACT_ID"
              gh artifact download $ARTIFACT_NAME --dir .
              IP=$(cat rancher_ip.txt)
              echo "RANCHER_URL=https://$IP.sslip.io" >> $GITHUB_ENV
              rm rancher_ip.txt
              exit 0
            fi
            echo "Attempt $i: Rancher not ready yet. Waiting 10 seconds..."
            sleep 10
          done
          echo "Tired of waiting for rancher-ip artifact. Timing out."
          exit 1
      - name: Wait for Rancher to be ready
        run: |
          echo "Waiting for Rancher to be ready at ${{ env.RANCHER_URL }}"
          timeout 5m bash -c 'until curl -k --silent --output /dev/null --head --fail "${{ env.RANCHER_URL }}/dashboard/"; do echo "waiting..."; sleep 5; done'
      - name: Setup Rancher and user
        run: |
          yarn e2e:prod
        env: 
          GREP_TAGS: ${{ matrix.role.tag }}Setup+${{ matrix.features[0] }} --@jenkins ${{ matrix.role.tag }}Setup+${{ matrix.features[1] || matrix.features[0] }} --@jenkins
          TEST_USERNAME: admin
          TEST_ONLY: setup
          TEST_BASE_URL: ${{ env.RANCHER_URL }}
          API: ${{ env.RANCHER_URL }}
      - name: Run user tests
        run: |
          yarn e2e:prod
          [ "$BUILD_DASHBOARD" != "false" ] || exit 0
        env: 
          TEST_SKIP: setup
          GREP_TAGS: ${{ matrix.role.tag }}+${{ matrix.features[0] }} --@jenkins ${{ matrix.role.tag }}+${{ matrix.features[1] || matrix.features[0] }} --@jenkins
          TEST_USERNAME: ${{ matrix.role.username }}
          VAI_ENABLED: ${{ (matrix.features[0] != '@noVai') && 'true' || 'false' }}
          TEST_BASE_URL: ${{ env.RANCHER_URL }}
          API: ${{ env.RANCHER_URL }}
      - name: Create Artifacts Folder
        if: ${{ always() }}
        run: |
          mkdir artifacts
          cp -r cypress/screenshots artifacts 2>/dev/null || :
          cp -r cypress/videos artifacts 2>/dev/null || :
          cp -r cypress/reports/* artifacts || :
          cp browser-logs/out.html artifacts/browser-logs.html 2>/dev/null || :
      - name: Upload Artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: ${{ always() }}
        with:
          name: ${{github.run_number}}-${{github.run_attempt}}-artifacts-${{ matrix.role.tag }}${{ matrix.features[0] }}${{ matrix.features[1] }}
          path: artifacts/
          if-no-files-found: error
          retention-days: 5
          compression-level: 9
      - name: Signal completion
        if: ${{ always() }}
        run: |
          echo "done" > completion.txt
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: ${{ always() }}
        with:
          name: test-done-${{ github.run_id }}-${{ matrix.role.username }}-${{ matrix.features[0] }}
          path: completion.txt
          retention-days: 1

  a11y-test:
    if: "!contains( github.event.pull_request.labels.*.name, 'ci/skip-e2e')"
    needs: e2e-ui-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - name: Setup env
        uses: ./.github/actions/setup
      - name: Wait for Rancher and get IP
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_ID: ${{ github.run_id }}
        run: |
          echo "Looking for artifact: rancher-ip-${RUN_ID}"
          for i in {1..60}; do
            ARTIFACT_ID=$(gh api "/repos/${{ github.repository }}/actions/runs/${RUN_ID}/artifacts" --jq ".artifacts[] | select(.name == \"rancher-ip-${RUN_ID}\") | .id")
            if [[ -n "$ARTIFACT_ID" ]]; then
              echo "Artifact found with ID: $ARTIFACT_ID"
              gh artifact download rancher-ip-${RUN_ID} --dir .
              IP=$(cat rancher_ip.txt)
              echo "RANCHER_URL=https://$IP.sslip.io" >> $GITHUB_ENV
              rm rancher_ip.txt
              exit 0
            fi
            echo "Attempt $i: Rancher not ready yet. Waiting 10 seconds..."
            sleep 10
          done
          echo "Tired of waiting for rancher-ip artifact. Timing out."
          exit 1
      - name: Wait for Rancher to be ready
        run: |
          echo "Waiting for Rancher to be ready at ${{ env.RANCHER_URL }}"
          timeout 5m bash -c 'until curl -k --silent --output /dev/null --head --fail "${{ env.RANCHER_URL }}/dashboard/"; do echo "waiting..."; sleep 5; done'
      - name: Setup Rancher and user
        run: |
          yarn e2e:prod
        env: 
          GREP_TAGS: "@adminUserSetup+@accessibility --@jenkins"
          TEST_USERNAME: admin
          TEST_ONLY: setup
          TEST_BASE_URL: ${{ env.RANCHER_URL }}
          API: ${{ env.RANCHER_URL }}
      - name: Run user tests
        run: |
          yarn e2e:prod
          [ "$BUILD_DASHBOARD" != "false" ] || exit 0
        env: 
          TEST_SKIP: setup
          GREP_TAGS: "@adminUser+@accessibility --@jenkins"
          TEST_USERNAME: admin
          TEST_A11Y: true
          TEST_NO_VIDEOS: true
          TEST_BASE_URL: ${{ env.RANCHER_URL }}
          API: ${{ env.RANCHER_URL }}

      - name: Upload report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: accessibility-report
          path: cypress/accessibility

  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      
      - name: Run tests
        uses: ./.github/actions/unit-tests

      - name: Collect Coverage
        run: |
          mkdir -p coverage-artifacts/coverage
          cp coverage/unit/coverage-final.json coverage-artifacts/coverage/coverage-unit.json
          cp -r coverage/unit/ coverage-artifacts/coverage/unit/

      - name: Upload coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{github.run_number}}-${{github.run_attempt}}-coverage
          path: coverage-artifacts/**/*

      - name: Upload unit test junit file
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{github.run_number}}-${{github.run_attempt}}-unit-tests
          path: unit-test-reports/*

  i18n:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1

      - name: Run i18n lint
        uses: ./.github/actions/i18n-lint

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1

      - name: Run tests
        uses: ./.github/actions/lint
      
  check-i18n:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
      
      - name: Install packages
        run: yarn install:ci
      
      - name: Run i18n string check
        run: |
          # Falure won't fail the job (remove -x when all current issues are fixed)
          ./scripts/check-i18n -s -x

  check-i18n-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
      
      - name: Install packages
        run: yarn install:ci
      
      - name: Run check of all http links in the i18n files (broken link check)
        run: |
          ./scripts/check-i18n-links

  # coverage:
  #   runs-on: ubuntu-latest
  #   needs:
  #     - unit-test
  #   steps:
  #     - uses: actions/checkout@v3
  #       with:
  #         fetch-depth: 1

  #     - uses: actions/setup-node@v6
  #       with:
  #         node-version-file: '.nvmrc'

  #     - name: Download Coverage Artifacts
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: ${{github.run_number}}-${{github.run_attempt}}-coverage

  #     - name: Install Codecov CLI
  #       uses : .github/workflows/scripts/install-codecov.sh
  #       with:
  #         codecov-version:
  #         codecov-version-checksum:

  #     - name: Upload tests coverage report to Codecov
  #       run: ./codecov --verbose upload-process -t ${{ secrets.CODECOV_TOKEN }} -n ${{github.run_number}}-${{github.run_attempt}}-coverage -F unit -f ./coverage/coverage-unit.json

  check-e2e-tags:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
      - name: Check e2e tags
        run: |
          ./scripts/check-e2e-tests-for-tags

  check-cypress-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: '.nvmrc'
      - name: Verify Cypress build
        working-directory: ./cypress
        run: |
          yarn install --frozen-lockfile
          yarn build-pkg --verify

  # Store the PR number in an artifact for use in TestHub registration since workflow_run triggered by PRs doesn't include the PR number in the head branch or commit message
  store-origin:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    steps:
      - name: Save PR number or Branch name
        env:
          PR_NUMBER: ${{ github.event.number }}
        run: |
          mkdir -p ./origin

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "Storing PR number ${PR_NUMBER} for TestHub registration"
            echo "pr/$PR_NUMBER" > ./origin/origin
          else
            echo "Storing branch name ${GITHUB_REF_NAME} for TestHub registration"
            echo "branch/$GITHUB_REF_NAME" > ./origin/origin
          fi

          cat ./origin/origin
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        name: Upload origin artifact
        with:
          name: origin
          path: origin/
