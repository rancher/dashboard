[
  {
    "default": [],
    "description": "This policy provides a list of allowed Flex Volume drivers. If the pod to be evaluated has a different driver on any `flexVolume` volume, it will be rejected.",
    "tooltip": "The `Key` needs to be `driver`, then provide a flex volume driver as the `Value`. (e.g. `driver: example/lvm`)",
    "group": "Settings",
    "label": "Allowed flex volumes",
    "target": true,
    "type": "map[",
    "variable": "allowed_flex_volumes"
  }
]