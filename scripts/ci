#!/bin/sh
set -e

BUILD_DEBUG="${BUILD_DEBUG:-}"
if [[ -n "${BUILD_DEBUG}" ]]; then
    set -x
    env
fi

cd $(dirname $0)

echo "Building..."
yarn run build

echo "Linting..."
yarn run lint

echo "Testing..."
yarn run test
