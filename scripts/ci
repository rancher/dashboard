#!/bin/sh
set -e

BUILD_DEBUG="${BUILD_DEBUG:-}"
if [[ -n "${BUILD_DEBUG}" ]]; then
    set -x
    env
fi

cd $(dirname $0)

echo "Cleaning up..."
rm -rf node_modules .nuxt

echo "Bootstrapping..."
yarn --pure-lockfile install

echo "Building..."
yarn run build
