import SchemaCache from '@shell/plugins/steve/caches/schemaCache';
import ResourceCache from '@shell/plugins/steve/caches/resourceCache';

export default {
  schemaCache:   SchemaCache,
  resourceCache: ResourceCache
};
