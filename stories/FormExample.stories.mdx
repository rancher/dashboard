import { Meta, Canvas, Story } from '@storybook/addon-docs';
import FormExample from './FormExample.vue';
<style>{`.labeled-select { font-size: unset' }`}</style>

<Meta
  title="Examples/Input Controls"
  parameters={{
    viewMode: 'docs',
    previewTabs: { 
      canvas: { hidden: true } 
    }
  }}
/>

# Form Example

<Preview>
  <Story name="Examples" inline>
  {{
    components: { FormExample },
    template: `<FormExample />`,
  }}
  </Story>
</Preview>
