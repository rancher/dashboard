import { Meta, Canvas, Story, ArgsTable, Source } from '@storybook/addon-docs';

import Labels from ''@shell/components/form/Labels';
import LabeledSelect from '@shell/components/form/LabeledSelect';



export const TemplateLabels = (args, { argTypes }) => ({
  components: { Labels },
  props:      Object.keys(argTypes),
  template:   '<Labels v-bind="$props" />',
})

export const TemplateLabeledSelect = (args, { argTypes }) => ({
  components: { LabeledSelect },
  props:      Object.keys(argTypes),
  template:   '<LabeledSelect v-bind="$props" />',
})

<Meta title="Test/Form" />

# Labels

Note: need to see usecase

<Source
  language='js'
  light
  format={false}
  code={`
     import Select from '../components/form/Select.vue';
  `}
/>

<Canvas>
  <Story
    name="Labels"
    args={{
      value: 'Select',
    }}>
    {TemplateLabels.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Labels} />


# LabeledSelect

Note: need to see usecase

<Source
  language='js'
  light
  format={false}
  code={`
     import LabeledSelect from '../components/form/LabeledSelect.vue';
  `}
/>

<Canvas>
  <Story
    name="LabeledSelect"
    args={{
      label: 'Select',
      options: ['Dummy', 'value']
    }}>
    {TemplateLabeledSelect.bind({})}
  </Story>
</Canvas>

<ArgsTable of={LabeledSelect} />
