import { Meta, Canvas, Story, ArgsTable, Source } from '@storybook/addon-docs';

import ToggleSwitch from '@shell/components/form/ToggleSwitch';

export const Template = (args, { argTypes }) => ({
  components: { ToggleSwitch },
  props:      Object.keys(argTypes),
  template:   '<ToggleSwitch v-bind="$props" />',
})

<Meta title="Components/Form/ToggleSwitch" />

# Title
Information about the component (subtitle)


<Source
  language='js'
  light
  format={false}
  code={`
     import ToggleSwitch from '@shell/components/form/ToggleSwitch';
  `}
/>

### Description
- bullet point 1
- bullet point 2
- bullet point 3
- bullet point 4

<Canvas>
  <Story
    name="ToggleSwitch"
    args={{
      value: '',
      offLabel: 'Dark mode: '
    }}>
    {Template.bind({})}
  </Story>
</Canvas>


### Props table
<ArgsTable of={ToggleSwitch} />
