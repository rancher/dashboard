import { Meta, Canvas, Story, ArgsTable, Source } from '@storybook/blocks';
import * as IconIsDefaultStories from './IconIsDefault.stories';

<Meta of={IconIsDefaultStories} />

# IconIsDefault

The `IconIsDefault` component is used to display an icon indicating whether a value is the default.

### Default

<Canvas of={IconIsDefaultStories.Default} />

### Is Icon Default

<Canvas of={IconIsDefaultStories.IsDefault} />

### Is Icon Not Default

<Canvas of={IconIsDefaultStories.IsNotDefault} />
