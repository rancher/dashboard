import { Meta, Canvas, Story, Controls } from '@storybook/blocks';
import * as RcButtonStories from './RcButton.stories';



# RcButton

Use RcButton for performing actions, such as submitting forms or opening dialogs. The button component supports different visual styles (variants), sizes, and can include icons.

<Canvas of={RcButtonStories.Default} layout="centered" />

<Controls of={RcButtonStories.Default} />

## Button Variants

Different button variants for different purposes and visual hierarchy:

<Canvas of={RcButtonStories.AllVariants} />

## As Router Link

When the `to` prop is provided, RcButton renders as an `<a>` tag (via Vue Router's `<RouterLink>`) instead of a `<button>`.
This enables client-side navigation while preserving all button styling.

The rendered HTML changes from:
- **Without `to`:** `<button class="rc-button btn variant-primary ...">` 
- **With `to`:** `<a class="rc-button btn variant-primary ..." href="/path">`

This improves accessibility and enables standard link behaviors like ctrl+click to open in a new tab.

<Canvas of={RcButtonStories.AsRouterLink} />

## Button Sizes

Buttons are available in three sizes: small, medium (default), and large:

<Canvas of={RcButtonStories.AllSizes} />

## Buttons with Icons

Buttons can include icons on the left, right, or both sides:

<Canvas of={RcButtonStories.WithIcons} />
